# Dovecot key settings (edit the relevant conf.d files as noted)

# /etc/dovecot/conf.d/10-mail.conf — line 24, uncomment:
mail_location = maildir:~/Maildir

# /etc/dovecot/conf.d/10-auth.conf — line 10, uncomment and set:
disable_plaintext_auth = no
# line 100, add login:
auth_mechanisms = plain login

# /etc/dovecot/conf.d/10-master.conf — line 110, uncomment and set:
# unix_listener /var/spool/postfix/private/auth {
#     mode = 0660
#     user = postfix
#     group = postfix
# }
